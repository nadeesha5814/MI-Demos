openapi: 3.0.1
info:
  title: HTTP-TCP
  description: API Definition of HTTP-TCP
  version: 1.0.0
servers:
- url: http://localhost:8290/http-tcp
paths:
  /:
    post:
      requestBody:
        description: Sample Payload
        content:
          application/json:
            schema:
              type: object
              properties:
                payload:
                  type: object
        required: false
      responses:
        default:
          description: Default response
